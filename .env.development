VITE_API_BASE_URL=https://api-stage-opto.vestor.uz

# Analytics — disabled for local development by default
VITE_ANALYTICS_ENABLED=false
VITE_ANALYTICS_ENV=local
# GTM IDs intentionally empty for local

# SEO — never indexed locally
VITE_SEO_INDEXING_ENABLED=false
VITE_PUBLIC_SITE_URL=http://localhost:8080
